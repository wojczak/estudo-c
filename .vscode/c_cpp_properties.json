{
    "version": 4,
    "configurations": [
        {
            "name": "MinGW",
            "compilerPath": "C:/MinGW/bin/gcc.exe", // ← GCC da mesma pasta
            "cStandard": "c11",
            "intelliSenseMode": "windows-gcc-x64",
            "includePath": [
                "${workspaceFolder}/include", // ← Adicione essa linha!
                "${workspaceFolder}"
            ]
        }
    ]
}